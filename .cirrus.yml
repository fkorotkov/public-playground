task:
  name: Test Things
  osx_instance:
    image: catalina-xcode-11.4-flutter
  version_script: 
    - which ruby
    - ruby --version
    - which bundle
    - bundle --version
  env_script: printenv
