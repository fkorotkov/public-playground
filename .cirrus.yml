build_task:
  container:
    image: alpine:latest
  script: echo "Foo"

task:
  name: Deploy to Staging
  depends_on: build
  trigger_type: manual
  container:
    image: alpine:latest
  script: echo "Bar"
