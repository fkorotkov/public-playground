task:
  alias: tests
  matrix:
    - name: Test Curl
      container:
        image: alpine/curl
  post_script:
    - echo "test" >> test.txt
    - curl -s -X POST --data-binary @test.txt http://${CIRRUS_HTTP_CACHE_HOST}/${CIRRUS_BUILD_ID}/test.txt
